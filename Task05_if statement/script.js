// Example variables for scores
var score1 = 45; // This score is expected to pass
var score2 = 30; // This score is expected to fail

// Check if score1 is a pass or fail
if (score1 >= 40) {
  console.log("Score1: " + score1 + " is a Pass.<br>");
} else {
   console.log("Score1: " + score1 + " is a Fail.<br>");
}

// Check if score2 is a pass or fail
if (score2 >= 40) {
  console.log("Score2: " + score2 + " is a Pass.<br>");
} else {
  console.log("Score2: " + score2 + " is a Fail.<br>");
}

// You can add more scores and tests if needed
